# Enable AndroidX and the Jetifier (migrate old support libs)
android.useAndroidX=true
android.enableJetifier=true

# Gradle JVM args (reduce or increase if you hit OOM)
org.gradle.jvmargs=-Xmx1024m -Dfile.encoding=UTF-8
